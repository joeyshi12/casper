name: Tests

on:
  push:
    branches: [main]
  pull_request:

# Cancel superseded runs on the same ref (e.g. rapid pushes to a PR).
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: test (node ${{ matrix.node-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # The package requires Node >=24 for node:sqlite; 26 catches breakage on the
        # line users will move to next.
        node-version: [24.x, 26.x]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - name: Install dependencies
        run: npm ci

      # tsx strips types without checking them, so the test run alone would miss
      # type regressions; type-check the workspaces explicitly.
      - name: Type-check
        run: npm run typecheck

      - name: Unit tests
        run: npm test
